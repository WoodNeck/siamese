const EMOJI = require('@/constants/emoji');
const { HISTORY } = require('@/constants/category');

module.exports = {
	name: HISTORY.NAME,
	description: HISTORY.DESC,
	categoryEmoji: EMOJI.BOOKS,
	commandEmoji: EMOJI.SMALL_WHITE_SQUARE,
};
