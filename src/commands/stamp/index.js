const EMOJI = require('@/constants/emoji');
const { STAMP } = require('@/constants/category');

module.exports = {
	name: STAMP.NAME,
	description: STAMP.DESC,
	categoryEmoji: EMOJI.WHITE_FLOWER,
	commandEmoji: EMOJI.PUSH_PIN,
};
