const EMOJI = require('@/constants/emoji');
const { FILE } = require('@/constants/category');

module.exports = {
	name: FILE.NAME,
	description: FILE.DESC,
	categoryEmoji: EMOJI.FLOPPY_DISC,
	commandEmoji: EMOJI.FILE,
};
