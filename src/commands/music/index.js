const EMOJI = require('@/constants/emoji');
const { MUSIC } = require('@/constants/category');

module.exports = {
	name: MUSIC.NAME,
	description: MUSIC.DESC,
	categoryEmoji: EMOJI.HEADPHONES,
	commandEmoji: EMOJI.UP_TRIANGLE,
};
