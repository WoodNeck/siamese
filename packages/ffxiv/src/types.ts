export interface ItemEntry {
  name: string;
  color: `#${string}`;
  icon?: string;
  url: string;
}

export interface CharacterEntry {
  type: string;
  label: string;
  server: string;
  link: string;
}
